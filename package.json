{
  "name": "wedding-site",
  "private": true,
  "description": "Tiaan & Nina wedding website — static HTML/CSS/JS",
  "scripts": {
    "build": "node scripts/build-public.mjs",
    "dev": "npx serve .",
    "preview": "npx serve ."
  },
  "devDependencies": {
    "serve": "latest"
  }
}
